name: concetour3d
description: >-
  ConceTour 3D — app de realidad aumentada para crear, compartir y recorrer
  rutas turísticas a pie por Concepción, Chile.
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  go_router: ^14.2.0
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
